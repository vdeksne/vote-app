sort1 uses: Bubble sort

How do you know?:

Because in comparison to the other sorting techniques, this one would be the slowest, such as in this example BigO(n^2)

sort2 uses: Merge sort

How do you know?: It takes the least time to switch because of the most minimal value of swapping numbers

sort3 uses: Selection sort

How do you know?: Since there is a lot of moving, it also takes quite a bit of time to finish the task

